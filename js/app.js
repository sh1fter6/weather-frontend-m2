/**
 * Punto de entrada principal de la App de Clima (Módulo 5)
 * Instancia la clase principal WeatherApp y arranca la aplicación.
 */
$(document).ready(function() {
  const app = new WeatherApp();
  app.iniciar();
});
